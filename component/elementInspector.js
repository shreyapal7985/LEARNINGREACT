//we have to run this command on terminal if we r using the real phone "adb shell input keyevent 82" or "just by shaking the phone"
//if we r using android emulator then we have just simply press ctrl+M in the terminal
//Inspect element is like we r go to inspect when we r using the website for getting the information about the sepecific element