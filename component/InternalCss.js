import { Text, View, StyleSheet, } from "react-native";

const Internal = () => {
    return(
        <View>
            <Text>Hello internal Css</Text>
        </View>
    );
};
 const styles=StyleSheet.create(
    {
        
    }
 )
 export default Internal;
